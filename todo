rename server.c to main.c to reflect it should only have main

move obj directories around for consistency between test and real

It's dumb to do all this in separate processes. I need to swap to threads.
